name: Merge me!

on:
  workflow_run:
    types:
      - completed
    workflows:
      - 'Continuous Integration'

jobs:
  merge-me:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    name: Merge me!
    runs-on: ubuntu-latest
    steps:
      - name: Merge me!
        uses: HuYo-OS/Resources@main
        with:
          GITHUB_TOKEN: ${{ secrets.MYGITHUB_TOKEN }}
    timeout-minutes: 5
